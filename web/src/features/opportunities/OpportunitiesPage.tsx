import { ArrowRight, Banknote, CalendarClock, Check, ExternalLink, FileSearch, MapPin, Search, Sparkles, X } from 'lucide-react'
import { AnimatePresence, motion } from 'motion/react'
import { useState } from 'react'
import { StatusBadge } from '../../components/StatusBadge'
import { displayValue, formatDate } from '../../lib/format'
import type { EnrichmentRun, MatchResult, MatchRun } from '../../lib/types'

export function OpportunitiesPage({ run, onMatch, matching, selected, onSelect, onPrepare, enrichment, onEnrich, onAcceptEvidence, busy }: {
  run?: MatchRun; onMatch: () => void; matching: boolean; selected?: MatchResult; onSelect: (result?: MatchResult) => void; onPrepare: (result: MatchResult) => void; enrichment?: EnrichmentRun; onEnrich: (policyId: string) => void; onAcceptEvidence: (id: string) => void; busy: boolean
}) {
  const [query, setQuery] = useState('')
  const results = (run?.results ?? []).filter(result => (result.title + result.benefit + result.agency).toLowerCase().includes(query.toLowerCase()))
  if (!run) return <EmptyMatching onMatch={onMatch} matching={matching} />
  return (
    <>
      <section className="page-heading split-heading"><div><span className="kicker">POLICY MATCHING · PASSPORT v{run.passport_version}</span><h1>Cơ hội được xếp hạng theo <em>khả năng hành động.</em></h1><p>Hybrid retrieval + reranking tìm cơ hội. Rule engine kiểm tra từng điều kiện.</p></div><button className="button secondary" onClick={onMatch} disabled={matching}><Sparkles />Chạy lại matching</button></section>
      <div className="search-toolbar"><div className="search-input"><Search /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Tìm trong kết quả…" aria-label="Tìm cơ hội" /></div><span><b>{results.length}</b> chính sách đã duyệt</span><span className="retrieval-mode">HYBRID · RRF + RERANK</span></div>
      <div className="opportunity-layout"><div className="opportunity-list">{results.map((result, index) => <motion.article className="opportunity-card" key={result.policy_id} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .05 }}><div className="match-score"><strong>{result.score}</strong><span>match score</span></div><div className="opportunity-main"><div className="opportunity-head"><div><span>{result.agency}</span><h2>{result.title}</h2></div><StatusBadge status={result.eligibility.status} /></div><p>{result.benefit}</p><div className="policy-meta"><span><Banknote />{result.benefit_amount || 'Theo chương trình'}</span><span><CalendarClock />Hạn {formatDate(result.deadline)}</span><span><MapPin />Việt Nam</span></div><div className="reason-row">{result.ranking_reasons.slice(0, 2).map(reason => <span key={reason}><Check />{reason}</span>)}</div></div><button className="card-open" aria-label={`Mở ${result.title}`} onClick={() => onSelect(result)}><ArrowRight /></button></motion.article>)}</div><aside className="panel match-explainer"><span>VÌ SAO KẾT QUẢ NÀY?</span><h3>Minh bạch từ tìm kiếm đến kết luận</h3><ol><li><b>01</b><p><strong>Metadata filter</strong>Lĩnh vực, địa phương, quy mô, deadline</p></li><li><b>02</b><p><strong>Hybrid retrieval</strong>Full-text + vector, hợp nhất bằng RRF</p></li><li><b>03</b><p><strong>Reranking</strong>Nhu cầu doanh nghiệp định hướng thứ tự</p></li><li><b>04</b><p><strong>Rule engine</strong>AI không quyết định eligibility</p></li></ol></aside></div>
      <AnimatePresence>{selected && <PolicyDrawer result={selected} onClose={() => onSelect(undefined)} onPrepare={() => onPrepare(selected)} enrichment={enrichment} onEnrich={() => onEnrich(selected.policy_id)} onAccept={onAcceptEvidence} busy={busy} />}</AnimatePresence>
    </>
  )
}

function EmptyMatching({ onMatch, matching }: { onMatch: () => void; matching: boolean }) {
  return <div className="matching-empty"><div className="orbit-mark"><Sparkles /></div><span className="kicker">COMPANY PROFILING HOÀN TẤT</span><h1>Tìm chính sách phù hợp với Passport hiện tại.</h1><p>P2B đối chiếu nhu cầu, lĩnh vực, địa phương, quy mô và điều kiện pháp lý. Eligibility không do LLM tự quyết.</p><button className="button primary" onClick={onMatch} disabled={matching}>{matching ? 'Đang retrieval & rerank…' : 'Bắt đầu policy matching'}<Search /></button><div className="matching-proof"><span><FileSearch />Nguồn chính thức</span><span><Check />Rule engine</span><span><Sparkles />Reranking có giải thích</span></div></div>
}

function PolicyDrawer({ result, onClose, onPrepare, enrichment, onEnrich, onAccept, busy }: { result: MatchResult; onClose: () => void; onPrepare: () => void; enrichment?: EnrichmentRun; onEnrich: () => void; onAccept: (id: string) => void; busy: boolean }) {
  const missing = result.eligibility.criteria.filter(item => item.status === 'MISSING_INFO')
  return <><motion.button className="drawer-scrim" aria-label="Đóng chi tiết" onClick={onClose} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} /><motion.aside className="policy-drawer" initial={{ x: '100%' }} animate={{ x: 0 }} exit={{ x: '100%' }} transition={{ type: 'spring', damping: 30, stiffness: 280 }}><header><div><span>POLICY DETAIL · v{result.policy_version}</span><h2>{result.title}</h2><p>{result.agency}</p></div><button aria-label="Đóng" onClick={onClose}><X /></button></header><section className="benefit-callout"><Banknote /><div><span>Quyền lợi</span><strong>{result.benefit_amount}</strong><p>{result.benefit}</p></div></section><section><div className="drawer-section-title"><h3>Kiểm tra điều kiện</h3><StatusBadge status={result.eligibility.status} /></div><div className="criteria-list">{result.eligibility.criteria.map(criterion => <article key={criterion.rule_id}><StatusBadge status={criterion.status} /><div><strong>{criterion.description}</strong><p>Quan sát: {displayValue(criterion.observed)} · Điều kiện {criterion.operator} {displayValue(criterion.expected)}</p><a href={criterion.citation.url} target="_blank" rel="noreferrer">{criterion.citation.source_name}<ExternalLink /></a></div></article>)}</div></section>{missing.length > 0 && <section className="missing-section"><div className="drawer-section-title"><h3>Dữ liệu còn thiếu</h3><span>{missing.length} mục</span></div><p>Agent chỉ tìm từ website doanh nghiệp và nguồn nhà nước được phép.</p><button className="button secondary wide" onClick={onEnrich} disabled={busy}><Search />Tìm evidence công khai</button>{enrichment && <div className="enrichment-list">{enrichment.candidates.map(candidate => <article key={candidate.id}><div><strong>{candidate.label}</strong><span>{displayValue(candidate.value)}</span><small>{candidate.warning}</small></div><button disabled={candidate.status === 'ACCEPTED' || busy} onClick={() => onAccept(candidate.id)}>{candidate.status === 'ACCEPTED' ? 'Đã nhận' : 'Xác nhận'}</button></article>)}</div>}</section>}<footer><span>{result.template_ready ? 'Có template PDF đã duyệt' : 'Chưa có template PDF'}</span><button className="button primary" onClick={onPrepare} disabled={!result.template_ready}>Chuẩn bị hồ sơ<ArrowRight /></button></footer></motion.aside></>
}

